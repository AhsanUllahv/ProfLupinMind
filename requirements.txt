# ProfLupinMind requirements
# Keep this file simple for first-time setup:
#   pip install -r requirements.txt
#
# Notes:
# - "Core runtime" is enough to run MCP + API + session/report features.
# - Optional dependencies are listed commented out to avoid install hurdles.

# ---------------------------------------------------------------------------
# Core runtime (required)
# ---------------------------------------------------------------------------
anthropic>=0.25.0
click>=8.1.0
flask>=3.0.0
httpx>=0.27.0
jinja2>=3.1.0
mcp>=1.0.0
requests>=2.31.0
sqlalchemy>=2.0.0
starlette>=0.37.0
uvicorn>=0.27.0

# ---------------------------------------------------------------------------
# Optional features
# ---------------------------------------------------------------------------
# PDF export for reports (reports/generator.py).
# WeasyPrint may require extra system libraries on some OS images.
# Uncomment if you want PDF generation:
# weasyprint>=60.0
